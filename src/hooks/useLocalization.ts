import { useContext } from 'react';
// import { LocalizationContext } from '~theme/localization';

const useLocalizationString = () => {
  // const LocalizationString = useContext(LocalizationContext);
  // return LocalizationString;
};

export default useLocalizationString;
